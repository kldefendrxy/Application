<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">89.111.170.165</domain> <!-- ВАШ IP-АДРЕС -->
        <domain includeSubdomains="true">10.0.2.2</domain>      <!-- Для эмулятора Android -->
        <!-- Можете добавить другие домены/IP, если нужно -->
    </domain-config>
    <!-- Можно добавить базовую конфигурацию, если нужно разрешить для всех (НЕ РЕКОМЕНДУЕТСЯ) -->
    <!--
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
     -->
</network-security-config>